flask-template
==============
Flask+Bootstrap boilerplate

# Requirements:
You can install it with pip
* flask-sqlalchemy
* flask-assets
* flask-wtf

# Optional:
Not required, but preferably
* alembic